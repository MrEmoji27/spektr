<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- MediaProjection capture on Android 14+ requires the FGS type and this
         permission; the service declares foregroundServiceType="mediaProjection". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AudioPlaybackCapture needs this as well as the projection token, and
         the distinction is easy to miss: the token alone is enough to capture
         the *screen*, but an AudioRecord built from an
         AudioPlaybackCaptureConfiguration is still an audio record and the
         platform refuses it without RECORD_AUDIO. Dangerous permission, so it
         is requested at runtime too — see MainActivity.onStartCaptureClicked. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".SpektrApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spektr">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".CaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
    </application>

</manifest>
